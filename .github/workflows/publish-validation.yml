name: publish-validation
run-name: Publish Validation

on:
  workflow_dispatch: {}
  push: {}

jobs:
  publish:
    runs-on: ${{ vars.RUNNER }}

    steps:
      - name: Echo
        run: echo "ANDROID_HOME = $ANDROID_HOME | env.ANDROID_HOME = ${{ env.ANDROID_HOME }}"